<?xml version="1.0" encoding="UTF-8"?>
<!-- Logging config: console + rolling file (evaluation requires ~1 week retention) -->
<configuration>
    <property name="LOG_DIR" value="logs" />
    <property name="LOG_FILE" value="pkc-api" />

    <!-- Hint: shows logs in IntelliJ Run window -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Hint: daily file roll; old files compressed to .gz -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${LOG_FILE}.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <!-- Hint: keep 7 days — matches non-functional requirement -->
            <maxHistory>7</maxHistory>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Hint: DEBUG for our packages; change to INFO in production if too noisy -->
    <logger name="com.oraclequantapi" level="DEBUG" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ROLLING_FILE" />
    </root>
</configuration>
