package com.oraclequantapi.oraclequantapi;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
/**
 * Basic Spring Boot startup test.
 *
 * If this test passes, Spring can create the application context with the
 * controllers, services, repositories, and configuration.
 */
class OraclequantapiApplicationTests {

	@Test
	void contextLoads() {
		// Empty on purpose: Spring Boot fails the test automatically if startup breaks.
	}

}
